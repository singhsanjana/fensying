release sequence algorithm
for i in trace
 if i is write & release
  get all direct sb
  if it has rf, add dob

  get all direct mo's
  if it has rf and is not rlx, add dob

  for this write, goto 3 and add dob for i
